{% extends 'base.html.twig' %}

{% block body %}
    <h1>Klanten</h1>

    <table>
        <thead>
            <tr>
                <th>Voornaam</th>
                <th>Achternaam</th>
                <th>Straat</th>
                <th>Huisnummer</th>
                <th>Postcode</th>
                <th>Woonplaats</th>
                <th>Telefoonnummer</th>
                <th>E-mail</th>
                <th>Allergiën</th>
                <th>Bewerkingen</th>
            </tr>
        </thead>
        <tbody>
        {% for customer in customers %}
            <tr>
                <td>{{ customer.firstName }}</td>
                <td>{{ customer.lastName }}</td>
                <td>{{ customer.street }}</td>
                <td>{{ customer.houseNumber }}</td>
                <td>{{ customer.postalCode }}</td>
                <td>{{ customer.city }}</td>
                <td>{{ customer.telephone }}</td>
                <td>{{ customer.email }}</td>
                <td>{{ customer.allergies }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('customer_show', { 'id': customer.id }) }}">Inzien</a>
                        </li>
                        <li>
                            <a href="{{ path('customer_edit', { 'id': customer.id }) }}">Bewerken</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('customer_new') }}">Nieuwe klant</a>
        </li>
    </ul>
{% endblock %}
